version: '3.5'

services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:6.3.2
    environment:
      - discovery.type=single-node
    ports:
      - 9200:9200
    networks:
      - gfgnetwork

networks:
  gfgnetwork:
    name: gfgnetwork
